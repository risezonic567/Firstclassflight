version: "3.9"

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: frontend-first-class  
    ports:
      - "5173:5173"  
    restart: always

    volumes:
      - ../Firstclassflight:/app
      - /app/node_modules
